# NMK Помощник

Telegram-бот на [aiogram](https://aiogram.dev/), помогающий студентам НМК узнавать расписание занятий, делиться домашними заданиями и управлять доступом через админ-панель.

## Возможности
- 📅 Получение расписания на сегодня, завтра или целую неделю с разбором официального веб-расписания. Автоматически поддерживает выбор группы по умолчанию и временный ввод группы для разовых запросов.
- 📚 Работа с домашними заданиями: личные и публичные списки, очередь на модерацию, базовая AI-проверка с логированием. Файлы и настройки создаются при первом запуске.
- 🛡️ Админ-панель с уровнями доступа, рассылками, логами, блокировкой пользователей и управлением категориями функций бота.
- 🚫 Обязательное подтверждение условий использования и ведение логов ошибок пользователей для диагностики.
- 🤖 Набор команд для обычных пользователей и администраторов.

## Требования
- Python 3.11+ (рекомендуется использовать виртуальное окружение)
- Токен Telegram-бота
- Дополнительно: ключ FreeImage и токен Telegraph для расширенных функций работы с домашкой (опционально)

## Установка
1. Клонируйте репозиторий и перейдите в директорию проекта.
2. Создайте и активируйте виртуальное окружение (рекомендуется):
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # Linux/macOS
   .venv\\Scripts\\activate   # Windows PowerShell
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

## Конфигурация
Основные настройки считываются из `cfg/bot_config.json`. При первом запуске папки `cfg` и `config` создаются автоматически с заготовками.

Пример `cfg/bot_config.json`:
```json
{
  "bot_token": "123456:ABCDEF...",
  "freeimage_api_key": null,
  "telegraph_token": null
}
```

Другие файлы в `config/` (группы, алиасы, расписание, пароли админов, файлы логов и каталоги домашек) будут созданы автоматически, если отсутствуют. База данных SQLite хранится в `config/nmk_bot.db`.

## Запуск
Запустите поллинг бота:
```bash
python main.py
```
Бот поднимет хранилище FSM на SQLite, зарегистрирует роутеры и команды, после чего начнёт принимать обновления.

## Полезные сведения
- Все временные и лог-файлы находятся в `config/` и создаются при старте.
- Каталоги для домашек: `config/homeworks/personal` и `config/homeworks/public`. Логи AI-проверки пишутся в `config/homeworks/ai_logs.jsonl`.
- При ошибках пользователи видят сообщение о необходимости принять условия использования; сами ошибки сохраняются в `config/user_errors.log`.

